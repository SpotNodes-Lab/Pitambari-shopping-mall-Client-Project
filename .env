# Loaded by `npm run dev` and `vite build`. `.env.production` applies only at build time.
# Without this, dev defaults to `/api` (Vite proxy to localhost:4000).
# Override locally (gitignored): `.env.local` — e.g. VITE_API_URL=/api for local backend.
VITE_API_URL=https://api.pitambariindia.com
