# Pitambari CMS / homepage API (see USER_WEB_API.md in monorepo root).
# Dev with Vite proxy (vite.config): requests go to same origin /api -> proxy target.
VITE_API_URL=/api

# If the CMS runs on another host/port without proxy:
# VITE_API_URL=http://localhost:4000/api

# Rewards / loyalty server vars (see README.md) — separate from CMS.
